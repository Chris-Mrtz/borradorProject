class Informes:
    def __init__(self, nombre, tipo, fecha_generacion, generado_por):
        self.nombre = nombre
        self.tipo = tipo
        self.fecha_generacion = fecha_generacion
        self.generado_por = generado_por
